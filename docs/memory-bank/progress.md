# Progress

Current status (2026-03-15): Steps 0–10 and Steps 11.1–11.2 are complete and Husky pre-commit integration is in place. Step 10 (10.1–10.3) required no code changes — the shared footer already rendered visible attribution with the Malmö stad source link on every page (index, detail, and comparison). The repo now ships a fully data-backed comparison view at `/sv/jamfor/`: the Astro route loads all Malmö survey files at build time, and the `client:only="preact"` `ComparisonView` island renders the empty state for 0 selections, a prompt plus one-school results for 1 selection, and a side-by-side Helhetsbedömning comparison experience for 2–5 selections. Step 7.4 refined the comparison table for the iPhone 13 mini target viewport with real horizontal overflow and a sticky question column, Step 8.1 layered visible question headings, reusable pattern-filled SVG bar charts, and chart-adjacent data tables onto each comparison question, and Step 8.2 completed the color-blind-safe five-pattern palette with distinct solid, diagonal, dotted, horizontal-line, and crosshatch encodings. The compare tray CTA for Swedish remains a live navigation link, and the clear-on-comparison-page regression remains fixed. Step 8.3 added a visible legend per chart, and the Step 8.3 hardening follow-up extracted a shared `renderPatternContent` helper so legend swatches and chart patterns can never drift structurally. Step 8.4 added a static `<noscript>` fallback (with localized `compare.noscriptMessage` i18n key) directing users to individual preschool detail pages when JavaScript is unavailable, and gave each chart-adjacent data table a deterministic `id` for future `aria-describedby` wiring. Step 9.1 added deterministic comparison-summary computation in `src/features/comparison/summary.ts`: it computes all unique preschool pairs, classifies each matched Helhetsbedömning question by a fixed 5-point threshold, anchors every pair to the first selected survey's visible question set, and omits pairs with no matched questions. Step 9.2 added localized deterministic summary sentence formatting in `src/features/comparison/summaryText.ts`, extended the locale `summary.*` templates with explicit percentage placeholders, and expanded formatter coverage across higher/lower/similar cases, multi-question and multi-pair summaries, float rounding, and non-Swedish locale smoke checks. A follow-up hardening pass closed the remaining locale gaps by adding explicit English `lower` coverage, Arabic `lower` and `similar` coverage, and a direct summary-placeholder contract across `sv`, `en`, and `ar`. Step 9.3 now renders those formatted deterministic sentences in the comparison UI for 2+ selected preschools, keeps the section hidden for 0 or 1 selected preschool, locks the behavior with route-level Playwright coverage in `tests/e2e/comparison-page-route-shell.spec.ts`, and exposes the summary list as a labeled region with a localized heading for easier screen-reader navigation. Step 11.1 adds a dedicated `tests/e2e/accessibility-axe-core.spec.ts` suite that runs `@axe-core/playwright` audits against the Swedish directory page, a generated Swedish preschool detail page, and the comparison page with a seeded 2-school session state, all scoped to `wcag2a` and `wcag2aa` and guarded so client-rendered UI is present before the scan starts. Step 11.2 adds a dedicated `tests/e2e/keyboard-navigation-focus-ring.spec.ts` suite with 8 Playwright tests covering focus-visible styling, Tab reachability, Enter/Space operability, compare-tray keyboard behavior, comparison-page back-link behavior, and the intentional non-tabbability of charts and the read-only comparison table. Shared `getFocusRingContract` and `getFocusOutlineContract` helpers now live in `tests/e2e/fixtures.ts` so future keyboard audits can reuse the same CSS inspection logic. `pnpm validate` is green with 75 unit tests passing.

## Decision Log

### 2026-03-04: `minimumReleaseAge` reduced from 7 days to 3 days

- **Change**: `pnpm-workspace.yaml` `minimumReleaseAge` lowered from `10080` (7 days) to `4320` (3 days).
- **Rationale**: Automated scanners (Socket.dev, Snyk, npm audit) and community reports typically flag malicious packages within 24–48 hours. 3 days provides margin beyond that window while reducing friction for dependency updates.
- **Risk acknowledged**: Scanner detection is probabilistic, not guaranteed. A 3-day window is shorter than the previous 7-day window, which means a sophisticated, slow-to-detect attack has a narrower but non-zero chance of slipping through.
- **Mitigating factors**: This is a low-sensitivity static site with no backend, no user accounts, no secrets at runtime, and no external API calls. The blast radius of a compromised dependency is limited to build-time code execution and static output. Weekly Dependabot schedule further reduces exposure to very-new releases. The `trustPolicy: no-downgrade` setting prevents trust-level regressions.
- **Override mechanism**: For critical hotfixes that need a freshly-published package, add it to `minimumReleaseAgeExclude` in `pnpm-workspace.yaml` temporarily.

## Current Priorities

1. **Step 11.3** — Lighthouse verification.

## Next Focus

- Step 11.3 Lighthouse verification.
